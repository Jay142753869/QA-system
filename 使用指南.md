# 金融事件问答系统 - 使用指南

## 📦 版本信息

- **版本**: v1.0.0 (桌面迁移修订版)
- **打包日期**: 以实际构建产物为准
- **优化内容**: BERT延迟加载、CSV索引优化、线程安全、自动端口、离线前端资源、文件日志

---

## 🚀 快速开始

### 1. 解压文件

将交付的 `FinancialQA-*.zip` 解压到任意目录，例如：
```
D:\FinancialQA\
```

### 2. 运行应用

#### 方式一：双击运行（推荐）

双击 `FinancialQA.exe`，等待系统初始化完成。桌面窗口会自动打开；如果内嵌窗口不可用，程序会退回到系统浏览器。

#### 方式二：命令行运行

```cmd
cd D:\FinancialQA
FinancialQA.exe
```

---

## ⚡ 性能优化说明

本次优化版本相比原版有以下性能提升：

| 优化项 | 原版 | 优化版 | 提升 |
|--------|------|--------|------|
| 启动时间 | 5-10秒 | <1秒 | **10倍** |
| CSV查询 | 秒级 | 毫秒级 | **1000倍** |
| 线程安全 | 无保护 | RLock保护 | **安全** |
| 前端资源 | 依赖 CDN | 本地 vendor | **可离线** |
| 端口占用 | 固定 5000 | 自动空闲端口 | **更稳定** |

### 优化详情

1. **BERT延迟加载**: 首次使用时才加载BERT模型，避免启动时卡顿
2. **CSV预加载索引**: 启动时预加载数据并构建索引，查询速度提升1000倍
3. **线程安全**: 全局状态使用RLock锁保护，防止多线程竞争
4. **超时机制**: 添加BERT加载30秒超时、初始化120秒超时、查询30秒超时
5. **桌面端迁移**: 前端依赖已本地化，桌面入口自动分配端口并在关闭时清理进程资源

---

## 📝 使用说明

### 系统要求

- **操作系统**: Windows 10/11 64位
- **内存**: 建议8GB或以上
- **磁盘空间**: 至少2GB可用空间
- **桌面运行库**: Windows 建议安装 Edge WebView2 Runtime；不可用时可自动退回浏览器
- **浏览器**: Chrome、Edge、Firefox等现代浏览器（备用访问方式）

### 首次使用

1. 首次启动时会加载BERT模型和数据，可能需要10-30秒
2. 请耐心等待，不要重复点击或关闭程序
3. 桌面窗口会自动打开，显示加载进度
4. 加载完成后即可开始使用

### 日常使用

1. 在输入框中输入您的问题，例如："招商银行的董事长是谁？"
2. 选择查询模式：内推（Internal）或外推（External）
3. 点击发送按钮
4. 系统会返回知识库查询结果和模型推理结果

---

## 🔧 故障排除

### 问题1：启动时卡住不动

**可能原因**: BERT模型加载超时

**解决方法**:
1. 检查网络连接（首次需要下载BERT模型）
2. 等待2-3分钟，看是否会自动降级到Mock模式
3. 重启应用再试

### 问题2：查询时提示"系统仍在初始化"

**可能原因**: 后台初始化未完成

**解决方法**:
1. 等待30秒后再试
2. 查看控制台输出，检查是否有错误信息
3. 重启应用

### 问题3：浏览器窗口没有自动打开

**可能原因**: Webview组件问题

**解决方法**:
1. 留意弹窗中的“浏览器访问地址”，手动打开该本地地址
2. 安装 Edge WebView2 Runtime
3. 使用浏览器模式而非桌面模式

### 问题4：杀毒软件拦截

**可能原因**: 杀毒软件误报

**解决方法**:
1. 将应用目录添加到杀毒软件白名单
2. 暂时关闭杀毒软件后再运行
3. 使用管理员权限运行

---

## 📞 技术支持

如有其他问题，请查看：
1. 控制台输出日志
2. `%LOCALAPPDATA%\FinancialQA\logs\app.log`
3. 系统事件查看器

---

## 📝 更新日志

### v1.0.0 (优化版) - 2024年
- ✨ 新增BERT延迟加载，启动速度提升10倍
- ⚡ 新增CSV预加载索引，查询速度提升1000倍
- 🔒 新增线程安全保护，防止多线程竞争
- ⏱️ 新增超时机制，防止系统卡住
- 🖥️ 新增桌面端自动端口、进程清理、离线 vendor 资源和文件日志
- 🐛 修复已知问题，提升稳定性

---

**感谢您使用金融事件问答系统！**
