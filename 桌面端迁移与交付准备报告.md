# 桌面端迁移与交付准备报告

生成日期：2026-06-24

## 一、结论

这个应用可以迁移到其他 Windows 主机运行，并且可以按桌面端交付；当前项目已经具备桌面版基础：`gui_launcher.py` 使用 pywebview 打开本地 Flask 服务，`FinancialQA.spec` 也已经把模板、静态文件、RE-GCN、TiRGN 数据与权重纳入 PyInstaller 打包。仓库中还存在历史发布包 `release/FinancialQA-v0.0.9-win-x64.zip`，大小约 2.9GB，说明这条路线已经实践过。

但它现在还不能被视为稳定、可复制、可给其他主机直接使用的桌面应用。除了前一份审计报告中的 bug，还必须补齐运行环境冻结、离线资源、模型依赖、首启验证、端口/进程管理、日志与用户数据目录、目标机兼容性测试等交付工程。

推荐目标形态：Windows x64 桌面 one-folder 安装包，而不是源码迁移；用户双击 `FinancialQA.exe`，程序在本机后台启动 Flask，只监听 `127.0.0.1` 动态端口，pywebview 作为桌面窗口承载界面。不要让用户安装 Python、Conda 或手动执行 `python app.py`。

## 二、当前迁移基础

### 已具备

- `gui_launcher.py` 已提供桌面入口，通过 pywebview 创建窗口。
- `app.py` 支持 frozen 模式，会从 `sys._MEIPASS` 读取 `templates` 和 `static`。
- `config.py` 使用 `BASE_PATH` 组装模型与数据路径，理论上能适配 PyInstaller 解包目录。
- `FinancialQA.spec` 已将以下资源加入打包：
  - `templates`
  - `static`
  - `models/RE-GCN-master/src`
  - `models/RE-GCN-master/rgcn`
  - `models/RE-GCN-master/data/80STOCKS`
  - RE-GCN 权重
  - `models/TiRGN-main/src`
  - `models/TiRGN-main/rgcn`
  - `models/TiRGN-main/data/80STOCKS`
  - TiRGN 权重
- `build_app.py` 已提供 PyInstaller 打包与 zip 发布包生成逻辑。

### 当前证据

- `release/FinancialQA-v0.0.9-win-x64.zip` 存在，大小约 2926MB。
- 模型资源体量较大：
  - `models/RE-GCN-master` 约 89MB
  - `models/TiRGN-main` 约 441MB
  - 发布包远大于模型文件，主要来自 PyTorch、DGL、Transformers 等运行库。
- 当前可用开发解释器是：
  - `C:\Users\Jay14\anaconda3\envs\regcn_clean\python.exe`
  - 默认 `python` 并不可用。

## 三、必须补齐的迁移工作

### 1. 固化运行环境，避免目标机安装 Python

迁移到其他主机时，不能依赖对方机器已有 Python、Conda、CUDA、pip 或 Hugging Face 缓存。必须以 PyInstaller 打包后的 one-folder 作为正式交付物。

当前依赖问题：

- `requirements.txt` 不完整，缺少 PyInstaller 打包实际需要的 `dgl`、`scipy`、`networkx`、`scikit-learn` 等依赖说明。
- 当前 Conda 环境中 `sklearn` 未安装，但 `FinancialQA.spec` 把 `sklearn` 放进了 hiddenimports。若实际代码不需要，应删除；若需要，应补 `scikit-learn`。
- 模型子项目的 requirement 写的是老版本 `torch==1.6.0` / `dgl-cu102==0.5.2`，但当前可运行环境是 `torch=2.0.0+cu118` / `dgl=1.1.2+cu118`，文档和实际环境不一致。

必须做：

- 生成一份锁定版本的环境清单，例如 `requirements-runtime.txt` 或 `environment.yml`。
- 明确使用 CPU 版还是 CUDA 版依赖。
- 打包只以一个固定环境为准，不要让 README、requirements、模型子项目 requirement 各说各话。

### 2. 解决 BERT 模型离线问题

当前 `core/preprocessing.py` 会尝试加载 `bert-base-chinese`：

1. 先用 `local_files_only=True` 查本机 Hugging Face 缓存。
2. 找不到再联网下载。
3. 下载失败或超时后降级 mock。

这对迁移非常危险：目标机器如果没有网络、没有缓存，首次查询就会退化，且用户未必知道真实 BERT 没有启用。

必须二选一：

- 方案 A：把 `bert-base-chinese` tokenizer 和模型文件随包内置，并把 `model_name` 指到包内相对路径。
- 方案 B：正式桌面版默认不加载 BERT，只使用已缓存的 `relation_embs_*.npz` 或规则识别，并在 UI/日志中明确“离线轻量模式”。

如果系统要宣称“真实 BERT 关系匹配可用”，必须采用方案 A。

### 3. 前端资源本地化

`templates/index.html` 当前依赖 CDN：

- Bootstrap CSS/JS
- FontAwesome
- jQuery

桌面端迁移不能依赖公网 CDN，否则离线机器、校园/企业网络或防火墙环境下界面会残缺。

必须做：

- 将前端依赖放到 `static/vendor/`。
- 修改模板引用 `url_for('static', filename='vendor/...')`。
- 在 PyInstaller spec 中确认 `static/vendor` 被打包。

### 4. 明确桌面应用的端口策略

当前桌面端本质是“本地 Flask 服务 + pywebview 窗口”。这可以接受，但必须把它当成桌面内嵌服务管理。

当前风险：

- `gui_launcher.py` 固定使用端口 `5000`。
- 如果目标机已有服务占用 5000，桌面应用会启动失败或打开错误页面。
- `app.py` 直接运行时绑定 `0.0.0.0`，源码运行会暴露到局域网。

必须做：

- 桌面版只监听 `127.0.0.1`。
- 启动时动态寻找可用端口，例如 5000 不可用则 5001/随机端口。
- pywebview 打开实际端口，而不是硬编码 `http://127.0.0.1:5000`。
- 加单实例锁，避免用户多次双击启动多个服务进程。

### 5. 区分程序目录、资源目录和用户数据目录

当前缓存写入位置与 `BASE_PATH` 相关，例如 `data/cache/relation_embs_*.npz`。打包后如果程序被放到 `Program Files` 或只读目录，写缓存可能失败。

必须做：

- 只读资源放程序目录，例如模型权重、CSV、模板、静态文件。
- 可写内容放用户目录，例如：
  - `%LOCALAPPDATA%\FinancialQA\logs`
  - `%LOCALAPPDATA%\FinancialQA\cache`
  - `%LOCALAPPDATA%\FinancialQA\config`
- 日志、缓存、用户设置不要写入 PyInstaller 解包目录或安装目录。

### 6. 提供目标机首启自检

桌面迁移不是“能打开窗口”就算成功。必须在目标机上自动检查关键资源。

建议新增 `desktop_self_check.py` 或内置 `/api/diagnostics`：

- 检查模板和静态文件是否存在。
- 检查 RE-GCN 权重是否存在。
- 检查 TiRGN 权重是否存在。
- 检查 `entity2id.txt`、`relation2id.txt`、`time2id.txt`、CSV、history npz 是否存在。
- 检查 torch/dgl 能否 import。
- 检查是否能加载一个小批量模型或至少读取 checkpoint。
- 检查 BERT 是内置、本地缓存、还是 mock 降级。
- 检查本地端口是否正常。

首启失败时要在桌面窗口显示明确原因，而不是只停在“系统启动中”。

### 7. 建立目标机验收用例

迁移后需要一套固定验收用例，而不是靠人工随便问。

最低验收：

- 双击 `FinancialQA.exe` 能打开桌面窗口。
- 无外网时 UI 样式完整。
- `/api/status` 返回 ready。
- 内推样例：`招商银行的董事长是谁` 有知识库结果和模型结果。
- 外推样例：`招商银行的董事长是谁` 在 external 模式有 TiRGN 返回。
- 错误样例：无效实体、无效关系能给出清晰提示。
- 关闭窗口后后台 Flask 进程退出。
- 第二次启动不会因为端口或残留进程失败。

## 四、建议补齐的交付材料

### 1. 发布包结构

建议正式发布包结构：

```text
FinancialQA-vX.Y.Z-win-x64/
  FinancialQA.exe
  _internal/
  README-运行说明.md
  版本说明.md
  故障排查.md
  checksums.txt
```

如果使用安装器，则可考虑：

- `FinancialQA Setup.exe`
- 安装到用户目录或 Program Files
- 自动创建开始菜单快捷方式
- 卸载入口

### 2. 运行说明

运行说明应面向普通用户，不应要求 Python 命令。

必须写清：

- 支持系统：Windows 10/11 x64。
- 推荐内存：至少 8GB，建议 16GB。
- 是否需要 GPU：建议写“无需 GPU，默认 CPU 可运行”；如果发布 CUDA 包，则必须说明体积和兼容性。
- 是否需要网络：正式桌面版应写“无需联网”；如果 BERT 仍会下载，则不能这么写。
- 首次启动耗时：模型加载可能需要几十秒。
- 日志位置。
- 常见失败处理。

### 3. 技术交付说明

给维护者的技术文档需要包含：

- 构建环境 Python 版本。
- 完整依赖版本。
- 打包命令。
- 发布包生成命令。
- 模型和数据文件来源。
- 如何更新模型权重。
- 如何运行验收脚本。
- 如何判断 BERT 是否真实启用。

## 五、桌面端特有风险

### 1. WebView2 兼容性

pywebview 在 Windows 上通常依赖 Edge WebView2 Runtime。很多 Windows 10/11 机器已有，但不能完全假设。

建议：

- 在运行说明中列出 WebView2 Runtime 要求。
- 桌面窗口启动失败时自动打开默认浏览器只是兜底，不应作为正式体验。
- 更稳的做法是安装包检测并提示安装 WebView2 Runtime。

### 2. 杀毒软件误报和大包解压慢

PyInstaller + PyTorch + 大模型发布包接近 3GB，常见问题包括：

- 杀毒软件扫描导致首次启动慢。
- 企业终端拦截未知 exe。
- zip 解压时间长，用户误以为卡死。

建议：

- 生成 SHA256 校验值。
- 使用固定版本号和签名策略。
- 首启界面显示明确加载阶段。

### 3. GPU/CPU 依赖选择

当前可运行环境是 CUDA 版 torch/dgl，但目标机不一定有 NVIDIA GPU。即使代码会在无 CUDA 时切到 CPU，CUDA 版运行库也会显著增大发布包。

建议：

- 如果目标是通用迁移，优先制作 CPU 版发布包。
- 如果必须使用 GPU，单独发布 GPU 版，并标注 NVIDIA 驱动/CUDA 兼容要求。

## 六、和前一份 bug 报告的关系

迁移前必须先修复或规避这些高风险问题：

1. TiRGN / RE-GCN 同名包污染。
2. GraphDAO mock 回退错误事实。
3. BERT 超时状态机。
4. 默认 Python 命令不可用。
5. NLP 误识别无效关系。
6. `get_fact()` 无时间返回空。
7. 时间解析误拼接和误识别股票代码。
8. 前端隐藏后端错误原因。

这些不只是本机 bug；迁移后排障难度会更高，所以应在正式打包前解决。

## 七、建议实施路线

### 阶段 1：桌面运行稳定化

- 修复前一份审计报告中的高优先级 bug。
- 改为动态本地端口。
- 加单实例锁。
- 关闭窗口时确保 Flask 后台线程/进程退出。
- 错误状态在 UI 中可见。

### 阶段 2：离线资源完整化

- 本地化 Bootstrap、jQuery、FontAwesome。
- 决定 BERT 策略：内置模型或明确禁用联网下载。
- 将缓存和日志迁到 `%LOCALAPPDATA%`。

### 阶段 3：打包环境冻结

- 建立 `environment.yml` 或 `requirements-lock.txt`。
- 修正 `requirements.txt` 与实际依赖不一致。
- 明确 CPU/GPU 包。
- 清理无用 hiddenimports。
- 重新生成 `FinancialQA.spec` 或确认现有 spec 可重复构建。

### 阶段 4：发布包与验收

- 用干净环境重新运行 `build_app.py`。
- 在至少一台无源码、无 Conda、无 Hugging Face 缓存的目标机器上测试。
- 在无网络环境下测试 UI 和问答。
- 运行固定验收问题集。
- 输出 zip、校验值、版本说明和故障排查文档。

## 八、迁移就绪标准

只有满足以下标准，才建议说“可以迁移到其他主机作为桌面端运行”：

- 用户不需要安装 Python/Conda。
- 用户不需要手动安装 Python 包。
- 无外网时 UI 样式完整。
- 无外网时不会尝试下载 BERT，或下载失败不会悄悄降级。
- 双击 exe 能打开桌面窗口。
- 端口冲突不会导致启动失败。
- 内推和外推在目标机均可用。
- 关闭窗口后后台服务退出。
- 日志可定位。
- 发布包有版本号、校验值和验收记录。

## 九、最终建议

可以迁移，但建议不要以“源码文件夹 + requirements.txt”的形式迁移。这个项目包含深度学习运行库、两个时序图模型、较大的本地数据和桌面壳，正确交付方式应是经过验证的 Windows x64 桌面发布包。

下一步最值得做的是：先修复模型包污染和离线资源问题，再把前端 CDN、本地端口、用户数据目录、BERT 模型策略和环境锁定一次性整理好。这样后续迁移到其他电脑时，问题会从“到处缺依赖、靠经验排查”变成“按自检报告定位缺什么”。
