# 应用问题合并审计报告

本报告合并两轮审计结果：一轮来自提交的 pasted-text 问题清单，一轮来自本地复核与端到端验证。结论按“已复现/证据充分”的实际风险排序；未能坐实的问题单独列在末尾。

## 验证环境与命令

- 默认 `python --version` 为 `Python 3.14.5`，缺少 Flask 等项目依赖。
- 可运行环境为 `C:\Users\Jay14\anaconda3\envs\regcn_clean\python.exe`。
- 已运行：
  - `python -B -m compileall app.py config.py core gui_launcher.py train_model.py validate_runtime.py test_regcn_integration.py test_tirgn_integration.py test_query.py`
  - `test_regcn_integration.py`
  - `test_tirgn_integration.py`
  - `validate_runtime.py`
  - BERT 超时竞态小复现
  - 先外推再内推的模型包污染复现
  - NLP 时间/关系解析抽样
  - GraphDAO mock 回退抽样

## 严重问题

### 1. TiRGN 和 RE-GCN 同名包污染，调用顺序会导致内推崩溃

- 位置：`core/tirgn_wrapper.py:27-38`，`core/regcn_wrapper.py:24-32`
- 验证结果：已复现。
- 现象：先调用外推 `external_reasoning_tirgn()`，再调用内推 `internal_reasoning()`，RE-GCN 初始化失败：
  `RecurrentRGCN.__init__() missing 3 required positional arguments: 'h_dim', 'opn', and 'history_rate'`
- 原因：两个模型目录都有同名 `src` / `rgcn` 包。TiRGN 会清理并优先插入自己的路径；RE-GCN 后续 append 路径时可能导入 TiRGN 的类。
- 建议：隔离模型导入命名空间，或在每次加载某模型前清理同名模块并临时置顶对应路径，最好封装为安全的 import context。

### 2. GraphDAO mock 回退会返回错误事实

- 位置：`core/graph_dao.py:82-94`，`core/graph_dao.py:196-208`
- 验证结果：已复现。
- 现象：CSV 查不到结果后，在 `USE_MOCK_GRAPH=True` 时回退 `_mock_query()`；只要关系包含“大股东”，不检查实体就返回贵州茅台集团。
- 实测：
  - `贵州茅台 + 大股东` -> `中国贵州茅台酒厂(集团)有限责任公司`
  - `招商银行 + 大股东` -> 同样返回贵州茅台集团
  - `不存在公司 + 大股东` -> 同样返回贵州茅台集团
- 建议：如果本地 CSV 已加载，应避免再回退 hardcoded mock；或 mock 必须同时校验 entity/relation/time。

### 3. BERT 加载超时后可能永久停留在 mock 模式

- 位置：`core/preprocessing.py:94-158`
- 验证结果：已复现。
- 小复现结果：`_ensure_bert_loaded(timeout=0.1)` 超时后设置 `use_mock=True`，后台线程随后成功设置了 `model/tokenizer`，但 `use_mock` 仍为 `True`。
- 后果：真实 BERT 已加载但后续仍走 mock 向量，关系匹配质量下降；同时 `_loading=False` 可能允许重复加载。
- 建议：超时与后台成功需要同一状态机保护。成功加载时应能恢复非 mock，或超时后取消/忽略迟到结果，并明确不可恢复。

### 4. 默认启动命令与实际可运行解释器不一致

- 位置：`AGENTS.md:12`，`README.md:78`，`train_model.py:14`
- 验证结果：已复现。
- 现象：当前默认 `python` 是 3.14.5，`import flask` 失败；Conda `regcn_clean` 环境依赖齐全。
- 建议：提供 `start_app.ps1` 或文档明确 Conda 环境；避免交付说明只写 `python app.py`。

## 高优先级问题

### 5. NLP 关系回退会把无效问题误判成真实金融关系

- 位置：`core/preprocessing.py:501-538`
- 验证结果：已复现。
- 实测：
  - `招商银行首席厨师是谁` -> 关系识别为 `联席总裁`
  - `蔡洪平在招商银行担任什么职务` -> 关系识别为 `人事和薪酬委员会委员`，但知识库查询为空
- 建议：增加意图约束、候选关系白名单、阈值校准，以及“不支持关系”的显式返回；无效业务词不应强行映射。

### 6. `get_fact()` 无时间参数时始终返回空，行为与 `predict()` 不一致

- 位置：`core/regcn_wrapper.py:359-378`
- 验证结果：已复现。
- 实测：
  - `get_fact('招商银行','董事长',None)` -> `[]`
  - `get_fact('招商银行','董事长','20250101')` -> `['缪建民']`
  - `predict(..., None)` 会使用最新时间快照
- 影响：当 GraphDAO 不可用或查不到时，内部事实索引无法在“未指定时间”的常见查询里补位。
- 建议：`get_fact()` 与 `predict()` 使用一致的时间解析策略；无时间可选择最新快照或返回所有时间的最新事实。

### 7. 时间解析会拼接无效日期，并误把股票代码当时间

- 位置：`core/preprocessing.py:434-464`
- 验证结果：已复现。
- 实测：
  - `2024年5月到2023年招商银行的董事长是谁` -> time 为 `2024年5月2023年`
  - `股票代码600000的董事长是谁` -> time 为 `6000`
- 建议：时间解析应输出结构化对象或单一最可信时间，不应直接拼接；4 位年份应限制合理范围，例如 1900-2100，并避开股票代码上下文。

### 8. 前端按分数阈值过滤会隐藏错误原因

- 位置：`static/js/main.js:301-318`
- 验证结果：代码路径确认。
- 现象：后端的 `Input Error`、模型初始化失败等通常是 `score: 0.0`，前端可能过滤成“没有满足阈值的预测结果”，用户看不到真实错误。
- 建议：`source` 包含 `Error` / `Input Error` 时优先展示，不参与置信度过滤。

## 中优先级问题

### 9. Debug 模式绑定 `0.0.0.0`

- 位置：`app.py:258-259`
- 验证结果：代码路径确认。
- 风险：`FLASK_DEBUG=true` 时 Flask debugger 暴露到所有网卡，有远程调试风险。
- 建议：本地调试默认绑定 `127.0.0.1`；只有显式配置时才允许 `0.0.0.0`。

### 10. `USE_MOCK_MODELS` / `USE_MOCK_GRAPH` 不支持环境变量覆盖

- 位置：`config.py:19-22`
- 验证结果：代码路径确认。
- 影响：交付和排障时必须改源码切换 mock/real，不利于不同机器运行。
- 建议：使用环境变量解析布尔值，例如 `USE_MOCK_MODELS=true/false`。

### 11. 硬编码用户本机 Conda 路径

- 位置：`train_model.py:14`
- 验证结果：代码路径确认。
- 影响：换机器训练脚本不可用。
- 建议：改为环境变量 `ANACONDA_PYTHON_PATH` 或默认 `sys.executable`。

### 12. GraphDAO 初始化失败时错误提示会误导用户

- 位置：`app.py:217-225`
- 验证结果：代码路径确认。
- 现象：当 `head` 和 `relation` 识别成功但 `state.graph_dao is None` 时，前端会收到“未能识别明确的实体或关系”。
- 建议：拆分提示：识别失败、图谱服务不可用、图谱无数据应分别表达。

### 13. 外部 CDN 依赖影响离线/打包运行

- 位置：`templates/index.html:8-10`，`templates/index.html:161-163`
- 验证结果：代码路径确认。
- 影响：无网络或 CDN 不可访问时 Bootstrap、FontAwesome、jQuery 可能失败。
- 建议：打包时将前端依赖放入 `static/vendor/`。

### 14. 弱默认密钥和默认 Neo4j 密码

- 位置：`config.py:10`，`config.py:15`
- 验证结果：代码路径确认。
- 影响：本地演示问题较小，但不适合部署。
- 建议：生产模式下缺少 `SECRET_KEY` 应直接报错；默认 Neo4j 密码仅用于开发说明，不应作为静默默认。

## 低优先级 / 可维护性问题

### 15. `external_reasoning()` 是未使用的占位方法

- 位置：`core/reasoning.py:74-87`
- 验证结果：代码搜索确认 `app.py` 调用的是 `external_reasoning_tirgn()`。
- 建议：删除或改成兼容包装，避免维护者误用。

### 16. `_csv_cache` 无上限，但正常索引路径下影响有限

- 位置：`core/graph_dao.py:15`，`core/graph_dao.py:155-193`
- 验证结果：代码路径确认。
- 说明：当前 CSV 预加载成功时走 `_indexed_query()`，不会频繁使用 `_csv_cache`；只有索引失败退回全表扫描时才有增长风险。
- 建议：保留为低优先级，必要时加 LRU。

### 17. `/api/query` 缺少输入长度限制和速率限制

- 位置：`app.py:174-189`
- 验证结果：代码路径确认。
- 影响：本地桌面应用风险较低；若绑定公网或局域网多人使用，会放大模型推理 DoS 风险。
- 建议：增加最大问题长度、请求超时、并发队列或简单 rate limit。

### 18. `regcn_wrapper.py` 读取数字三元组文件未显式指定编码

- 位置：`core/regcn_wrapper.py:101`
- 验证结果：代码路径确认。
- 说明：该文件读取的是纯数字 `train/valid/test.txt`，实际中文乱码风险很低；`tirgn_wrapper.py` 对应读取已指定 `encoding='utf-8'`，原提交清单中把 TiRGN 也列为同类问题不准确。
- 建议：为一致性可补 `encoding='utf-8'`，但不是当前主要风险。

## 未坐实或需要降级的问题

### A. “PyTorch 推理非线程安全”不应作为已复现严重 bug

- 位置：`core/reasoning.py:23-45`，`core/reasoning.py:61`，`core/reasoning.py:97`
- 验证状态：未复现崩溃或错乱。
- 判断：缺少推理锁确实是并发风险，尤其第三方模型内部可能有缓存或非只读状态；但 PyTorch eval 推理并不必然线程不安全。建议作为中优先级并发稳定性改进，而不是已确认严重 bug。

### B. `_resolve_time_id("202")` 会匹配意外时间的说法不成立

- 位置：`core/regcn_wrapper.py:321-340`，`core/tirgn_wrapper.py:244-264`
- 验证状态：`招商银行202的董事长是谁` 在 NLP 层未识别时间；`_resolve_time_id` 对少于 4 位数字返回 `None`。
- 判断：部分年份如 `2025` 匹配最新日期是设计行为；真正问题在 NLP 时间抽取会误抓股票代码和拼接多段日期。

### C. 文档与测试声明不一致需要修正

- 位置：`README.md:23-25`，`测试指南.md:6-7`，`交付清单.md:19`
- 验证结果：已确认存在不一致。
- 说明：README 说 external 非 mock 未接真实外推，但代码和测试均已调用 TiRGN；RE-GCN 集成脚本虽然退出码为 0，但输出提示预期答案未进入 Top 结果，所以“所有模块测试通过”不够严谨。

## 建议修复顺序

1. 修复模型包污染：保证 TiRGN 与 RE-GCN 导入隔离。
2. 移除或严格限制 GraphDAO hardcoded mock 回退。
3. 修复 BERT 超时状态机。
4. 统一启动环境：新增/更新固定解释器启动脚本和 README。
5. 收紧 NLP 关系和时间解析，避免无效问题被强行映射。
6. 调整前端错误展示，错误项不参与置信度过滤。
7. 清理配置、文档与交付声明不一致。
